class Personagem {
  nome: string;
  nivel: number;
  vida: number;

  constructor(nome: string, nivel: number, vida: number) {
    this.nome = nome;
    this.nivel = nivel;
    this.vida = vida;
  }
}